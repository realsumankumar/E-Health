import { AppointmentResponse } from './appointment-response';

describe('AppointmentResponse', () => {
  it('should create an instance', () => {
    expect(new AppointmentResponse()).toBeTruthy();
  });
});
