import { PharmacyCurrentRecord } from './pharmacy-current-record';

describe('PharmacyCurrentRecord', () => {
  it('should create an instance', () => {
    expect(new PharmacyCurrentRecord()).toBeTruthy();
  });
});
